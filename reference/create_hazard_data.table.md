# Hazard data.table helper function

Hazard data.table helper function

## Usage

``` r
create_hazard_data.table()
```

## Value

A data.table used in various functions that wrap the hazard API
endpoints.

## Examples

``` r
if (FALSE) {
hazard_dt <- create_hazard_data.table()
}
```
